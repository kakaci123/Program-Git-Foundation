
@if (ViewData.ModelMetadata.IsRequired)
{
    @Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue, new { @class = "text-box single-line  date-picker", type = "color", required = "required" })
} 
else 
{
   @Html.TextBox("", ViewData.TemplateInfo.FormattedModelValue, new { @class = "text-box single-line  date-picker", type = "color" })
}

